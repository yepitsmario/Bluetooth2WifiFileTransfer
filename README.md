# Bluetooth2WifiFileTransfer

### Bluetooth is used for the initial handshake to connect 2 separate devices via wifi. After connection, the users can then browse their own directories to push files via TCP.
